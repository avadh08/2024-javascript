"use strict"; // treate js code as newer version code.

let name = "avadh"  // string
let age = 19  // number
let status = null;  // null
let address;  // undefined
let passport = true;  // boolean

// symbol => unique

// type of - used for find the type of any variable.

console.log(typeof age)
console.log(typeof (name))
console.log(typeof null)  // type of null is object. 
